# Core dependencies
numpy>=1.24.0,<2.0.0
tqdm
psutil
requests
aiohttp
aiohttp-cors
aiofiles
grpcio
grpcio-tools
uvicorn
websockets

# ML dependencies - 版本兼容性配置
# transformers 4.56+ 需要 torch 2.7+ 的 AuxRequest
# 使用兼容的版本组合
torch>=2.0.0,<2.7.0
transformers==5.3.0
tokenizers
safetensors
huggingface-hub
accelerate

# Image processing
pillow
opencv-python-headless

# Config and utils
pydantic
PyYAML
click
rich
colorama

# GPU detection
nvidia-ml-py

# Optional but useful
sentencepiece
protobuf>=4.21.0
scapy